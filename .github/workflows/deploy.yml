# This workflow has been superseded by the decoupled structure below.
# Logic lives in:
#   .github/actions/deploy-frontend/action.yml  ← shared composite action
#   .github/workflows/dev.yml                   ← triggered on push to dev
#   .github/workflows/prod.yml                  ← triggered on push to main
#
# This file is kept as a manual-trigger stub only. Safe to delete once you
# confirm dev.yml and prod.yml are working correctly.

name: Deploy Frontend (deprecated stub)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual run"
        required: false
        default: "manual"

jobs:
  notice:
    runs-on: ubuntu-24.04
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated."
          echo "Use dev.yml (push to dev) or prod.yml (push to main) instead."
